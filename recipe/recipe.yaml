schema_version: 1

context:
  name: pywis-topics
  version: "0.5.1"

package:
  name: ${{ name|lower }}
  version: ${{ version }}

source:
  url: https://pypi.org/packages/source/${{ name[0] }}/${{ name }}/${{ name | replace('-', '_') }}-${{ version }}.tar.gz
  sha256: 8851d35c61e6e6798fb3da79287e94a7f5a465f8b8d80105032c9628db891149

build:
  number: 0
  noarch: python
  script: ${{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  python:
    entry_points:
      - pywis-topics=pywis_topics:cli

requirements:
  host:
    - python ${{ python_min }}.*
    - pip
    - setuptools
  run:
    - python >=${{ python_min }}
    - click

tests:
  - python:
      imports:
        - pywis_topics
      pip_check: true
      python_version:
        - ${{ python_min }}
        - "*"
  - script:
      - pywis-topics --help

about:
  summary: pywis-topics is a utility to work with the WIS2 Topic Hierarchy
  license: Apache-2.0
  license_file: LICENSE
  homepage: https://github.com/wmo-im/pywis-topics

extra:
  recipe-maintainers:
    - akrherz
